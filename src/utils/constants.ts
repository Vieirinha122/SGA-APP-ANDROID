import AppSettings from "../types/types";

export const BASE_URL = "https://sga-sociiz.netlify.app";

export const DEFAULT_SETTINGS: AppSettings = {
  painelToken: "",
  kiosk: true,
  lastPage: "painel",
};
